spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gesfiliereniveau
    username: babagesfiliereniveau
    password: Sidy2002
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8082
